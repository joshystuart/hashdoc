{
  "name": "HashDoc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "pnpm --filter @hashdoc/web dev"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.2"
}
